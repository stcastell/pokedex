import React, {Fragment} from 'react';
import './App.css';

const App = () => { 

  // fetch('https://pokeapi.co/api/v2/pokemon/Ditto').then(res => res.json()).then(data => console.log(data)).catch(err => console.error(err));
  
  return (
    <Fragment>
      HELLO
    </Fragment>
  );
}

export default App;
